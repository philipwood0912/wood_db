const config = {
    host: "localhost",
    port: 8889, // 8889 for mac 3306 for windows
    uname: "root",
    pword: "root", // leave blank for windows
    database: "db_card"
}

module.exports = config;